source "https://rubygems.org"#RubyGems.orgという公式のgemリポジトリを指定.gemをインストールする際に使用されるデフォルトのリポジトリ
git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }#GitHubリポジトリをgemのソースとして使用する場合の設定。この行は、GitHubにホストされているgemをインストールする際に使用され

gem "kosi" # https://github.com/tbpgr/kosi #kosiというgemをインストールするための指定。このgemは、テーブルを簡単に描画するためのツール。GitHubリポジトリ（https://github.com/tbpgr/kosi）からインストールされる。
gem "rexml" # rexmlというgemをインストールするための指定。このgemは、Rubyの標準ライブラリで提供されているXMLパーサー.
gem "rspec" #このgemは、RSpecというテストフレームワーク,これを使用することで、Rubyアプリケーションのテストを効果的に記述、実行することができる。
gem "factory_bot" #このgemは、テストデータのファクトリを定義してテストを容易にするためのツール。
